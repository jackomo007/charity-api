{
  "name": "api-server",
  "version": "1.0.0",
  "description": "GraphQL API for CharityVest Dashboard",
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node src/infrastructure/server/index.ts",
    "build": "tsc",
    "start": "npm run build && node dist/infrastructure/server/index.ts"
  },
  "dependencies": {
    "express": "^4.18.2",
    "apollo-server-express": "^3.8.2",
    "graphql": "^16.6.0"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "ts-node": "^10.9.1"
  }
}
